#ifndef header_H
#define header_H

#include <vector>
#include <string>

struct studentas{
    std::string vard;
    std::string pav;
    int egz;
    std::vector<int> nd;    
    double vid;
    double med;
    double suma=0;
    double gal;
    double gal2;
};

#endif